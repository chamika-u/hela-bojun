# Hela Bojun – Admin Credentials Template
# Copy this file to .env (never commit .env to version control)
# Use these values to populate assets/scripts/config.js

ADMIN_USERNAME=admin
# Generate a SHA-256 hash: echo -n "yourpassword" | sha256sum
ADMIN_PASSWORD_HASH=sha256-hash-of-your-password
